# SplatWorld3D — first external-world test

Work in progress. This branch tests whether the address-conditioned operator plate can fit sparse rendered camera views from an external analytic 3D scene and generalize to held-out intermediate views. The teacher is no longer another operator plate.
